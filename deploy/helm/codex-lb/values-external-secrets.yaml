# External Secrets install mode — database URL and encryption key are materialized by External Secrets Operator.
postgresql:
  enabled: false

externalSecrets:
  enabled: true

migration:
  enabled: true
  schemaGate:
    enabled: true

# Required:
# externalSecrets.secretStoreRef.name
# Recommended:
# networkPolicy.ingressNSMatchLabels when networkPolicy.enabled=true
